import React from 'react';
import { Select } from '@abdt/ornament';
import { AppContext } from 'context/AppContext.Provider';

export const Filters: React.FC = () => {
    const { headCells, filters } = React.useContext(AppContext);

    return <></>;
};
